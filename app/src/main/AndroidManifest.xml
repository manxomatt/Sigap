<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.sigap">

    <uses-sdk android:minSdkVersion="19" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <android:uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_new"
        android:label="@string/app_name"
        android:theme="@style/AppTheme.AppBarOverlay">

        <!-- Splash Screen -->
        <activity
            android:name=".SplashScreen"
            android:label="@string/app_name"
            android:noHistory="true"
            android:screenOrientation="portrait"
            android:theme="@style/SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity -->
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login"
            android:noHistory="true"
            android:screenOrientation="portrait"
            android:theme="@style/LogIn" />

        <!-- Forget Password Activity -->
        <activity
            android:name=".ForgetPasswordActivity"
            android:label="@string/title_activity_forget_password"
            android:noHistory="true"
            android:screenOrientation="portrait"
            android:theme="@style/ForgetPassword" />

        <!-- Main Menu Activity -->
        <activity
            android:name="com.app.master.MainMenuActivity"
            android:label="@string/title_activity_main_menu"
            android:noHistory="true"
            android:screenOrientation="portrait"
            android:theme="@style/MainMenu" />

        <!-- Signup Activity -->
        <activity
            android:name=".SignupActivity"
            android:label="@string/title_activity_signup"
            android:noHistory="true"
            android:screenOrientation="portrait"
            android:theme="@style/SignUp" />

        <!-- Signup Verification Activity -->
        <activity
            android:name=".SignVerificationActivity"
            android:label="@string/title_activity_signup_verification"
            android:noHistory="true"
            android:screenOrientation="portrait"
            android:theme="@style/SignUpVerification" />

        <!-- Signup Verification Activity -->
        <activity
            android:name=".LiveChatActivity"
            android:label="@string/title_activity_live_chat_sigap"
            android:noHistory="true"
            android:screenOrientation="portrait"
            android:theme="@style/LiveChat" />

        <!-- Bantuan Terdekat Activity -->
        <activity
            android:name=".BantuanTerdekatActivity"
            android:label="@string/title_activity_bantuan_terdekat"
            android:noHistory="true"
            android:screenOrientation="portrait"
            android:theme="@style/BantuanTerdekat" />
        <activity android:name=".BantuanTerdekatPolisiActivity"
            android:label="@string/title_activity_bantuan_terdekat"
            android:noHistory="true"
            android:screenOrientation="portrait"
            android:theme="@style/BantuanTerdekat" />
        <activity android:name=".BantuanTerdekatPolisiDataActivity"
            android:label="@string/title_activity_bantuan_terdekat"
            android:noHistory="true"
            android:screenOrientation="portrait"
            android:theme="@style/BantuanTerdekat" />

        <!-- Tentang Polisi -->
        <activity android:name=".TentangPolresActivity"
            android:label="@string/title_activity_tentang_polres"
            android:noHistory="true"
            android:screenOrientation="portrait"
            android:theme="@style/TentangPolres"/>
    </application>

</manifest>